# ClockCalendar.widget

Displays the current time, date, and a 3-month calendar grid (previous, current, and next month) with the current day highlighted.

## Dependencies

None — uses only built-in JavaScript date functions.

## Configuration

No configuration needed. The widget works out of the box.
